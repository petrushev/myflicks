{% macro search_box(q, tr) %}
<div id="search_box" >
  <form action="/search" method="post" >
    <input type="input" name="q" value="{{q|e}}" /> 
    <input type="submit" value="Search" />
  </form>
</div>
{% endmacro %}

{% macro movie_widget(movie, meta) %}
<div>
{% if meta %}
  <h3>{{meta['Title']|e ~' ('~ meta['Year'] ~')'}}</h3>
  <div>Director: {{meta['Director']|e}}, Screenwriter: {{meta['Writer']|e}}</div>
  <div>Actors: {{meta['Actors']|e}}</div>
{% else %}
  <h3>{{movie.title|e ~' ('~ movie.year ~')'}}</h3>
{% endif %}
  <div>
    <img width=300 src="{{movie.get_image()}}" />
  </div>
</div>
{% endmacro %}
